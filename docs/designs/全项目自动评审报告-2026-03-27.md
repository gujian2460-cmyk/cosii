# 《Cosii 全项目自动评审报告（/autoplan 风格 · 中文）》

- **评审日期：** 2026-03-27  
- **评审范围：** 后端 `src/`、测试 `tests/` + `e2e/`、脚本 `scripts/`、小程序 `miniprogram/`、CI `.github/workflows/`、上线与规格文档 `docs/`  
- **代码基线：** `HEAD=8cf2139`（与 2026-03-26 报告同提交时可对照 diff）  
- **门禁实跑：** `npm run verify:gates` — **全部通过**（含 `smoke:miniprogram`）  
- **双声道外评：** 本文件为 **Cursor 单模型深度合成**；未调用 Codex / 独立子代理。若需 gstack 完整双声道流水，请在本地跑官方 `/autoplan` 并合并输出。

---

## 执行摘要（Final Gate 口径）

| 维度 | 结论 |
|------|------|
| **CEO（方向与范围）** | 当前重心应从「补 Tab/分包骨架」转向 **生产信任边界（微信会话）+ 运维治理（密钥/告警/日志）**；功能主干与自动化门禁已证明可维护。 |
| **Design（小程序 UI）** | 列表/详情/消息等 **状态分层（loading/error/正文）** 已落地；长期需按 `DESIGN.md` 做视觉一致性与触控规范抽检。 |
| **Eng（架构与测试）** | 后端模块化清晰，Vitest + Playwright 主链覆盖扎实；**GitHub CI 已与 `verify:gates` 对齐**（2026-03-27 起，见 `.github/workflows/ci.yml`）。 |

**总评：** `DONE_WITH_CONCERNS` — **可继续迭代与预发验证，但「直接上生产」仍被 P0（认证、密钥、可观测性运维项）挡住。**

---

## 需要做什么（本评审 → 行动清单）

> **读这一节即可：** 下列条目由全文 Phase 1–3、CI 对比与 Failure Modes 归纳；勾选即进度。细背景见后文对应章节。

### 一句话

**先把「谁能在生产调用 API」和「CI 是否等于本地门禁」做对，再谈功能迭代。** 其余是体验债与工程质量增强。

---

### P0 — 上线前必须完成（阻塞项）

| # | 你要做的事 | 为什么（评审依据） | 建议产出 / 对照 |
|---|------------|-------------------|-----------------|
| P0-1 | **微信登录与会话** — **已落实代码路径**：`POST /v1/auth/wechat-login`（code2session + `users.wx_openid`）、`Authorization: Bearer` 解析；**生产收紧**：`NODE_ENV=production` + `COSII_TRUST_DEV_HEADER=0` 时仅 Bearer。小程序：`useWeChatSession` + 存储 token（默认本地仍 `X-User-Id`）。**401 文案**：各受保护路由未登录时统一 `message: "Authentication required"`（`AUTH_REQUIRED_MESSAGE`），语义「未认证」 | 同上 | 部署时设置 `WECHAT_*`、`COSII_SESSION_SECRET`；**上线前真机** `wx.login` 联调 + `go-live-checklist` / `发布前30分钟检查单` 对应勾选项 |
| P0-2 | **按 `docs/go-live-checklist.md` 与 `docs/发布前30分钟检查单.md` 逐项落实**：生产 DB/路径、`LOCAL_CODE_PEPPER`、微信支付证书与回调 URL、密钥轮换策略 | CEO 摘要「运维治理」、2026-03-26 报告 C 节同类项 | 清单全绿或明确豁免记录 |
| P0-3 | **告警与日志**：结算/webhook 等失败可观测（路由到人或渠道）；保留样例日志便于排障 | 同上、可观测性缺口 | Runbook 或 on-call 说明 + 一次演练记录 |
| P0-4 | **CI 与本地门禁对齐** — **已落实**：`ci.yml` 在 `build` + Playwright 安装后执行 `npm run verify:gates`（含 test / critical / e2e / smoke） | 原「与 GitHub CI 的差异」节 | 合并后主分支 CI 日志应四门全绿；与 `CLAUDE.md` 门禁说明一致 |

---

### P1 — 强烈建议（防回归 / 收敛技术债）

| # | 你要做的事 | 为什么 | 对照文档或代码 |
|---|------------|--------|----------------|
| P1-1 | **生产构建护栏**：脚本或 CI 步骤禁止 `useMockOrders: true` 等 mock 开关出现在上传包/生产配置 | Failure Modes「假数据上生产」 | [工程质量优化五项-工程评审-2026-03-27.md](./工程质量优化五项-工程评审-2026-03-27.md) 条 4 |
| P1-2 | **小程序请求层契约**：抽出纯函数 + Vitest（信封解析分支）与可选快照 | 测试评审 GAP「parse 单测」 | 同上文档 条 1 |
| P1-3 | **小程序占位能力产品化**：真机图片上传、档期列表 API、发布链路打磨等（按产品优先级排期） | Phase 2「旅程 6/10」、2026-03-26 报告 A 节方向 | `miniprogram/`、`autoplan-five-tabs-pages-layout.md` |
| P1-4 | **错误态 DRY**：统一错误条 + 重试（behavior / template），减少各页复制粘贴 | Phase 2、Phase 3 代码质量 | 工程质量五项 条 2 |
| P1-5 | **文档与 routes 一致性检查**（CI 脚本 warn-only 起步） | 降低 CLAUDE.md 漂移 | 工程质量五项 条 5 |

---

### P2 — 优化与规模化（不阻塞首发）

| # | 你要做的事 | 为什么 |
|---|------------|--------|
| P2-1 | 小程序自动化（miniprogram-automator 或真机脚本） | 当前仅靠 smoke + 手测 |
| P2-2 | 新增 `docs/runbooks/trace-id-playbook.md`（或等价）：用 `trace_id` 串联访问日志与业务单号 | Phase 3「trace_id 已有、人读文档缺」 |
| P2-3 | 按 `DESIGN.md` 做一轮视觉/触控抽检（非像素合同） | Phase 2 设计系统一致 6/10 |
| P2-4 | SQLite 增长后的备份与迁移策略文档化 | Phase 3 架构「单点」备注 |

---

### 建议落地顺序（若只能排一条线）

```text
1) ~~P0-4  CI ≈ verify:gates~~ **已完成**（2026-03-27）
2) P0-1  微信会话 + 禁 dev 头   ← 最大安全与产品边界
3) P0-2 / P0-3  清单 + 告警日志 ← 并行可部分交给运维
4) P1-1  mock 护栏             ← 防低级生产事故
5) P1-2  parse 单测            ← 防信封变更静默破坏小程序
6) 其余 P1 / P2 按迭代容量插入
```

---

### 验收标准（本评审视角）

- **「可以发预发」最小集：** P0-4 完成 + P0-1 在预发环境可端到端登录（非 dev header）。  
- **「可以上生产」最小集：** P0-1～P0-3 全部完成且有 checklist/演练证据；P1-1 建议同步完成。  
- **「工程质量满意」：** 另见《工程质量优化五项》全文勾选。

---

*下文「优先级汇总」小节与上表一一对应，保留为速览；**以本节表格为执行真源**。*

---

## Phase 0 — 摄入与范围

### 已读/已对照

- `CLAUDE.md`（模块清单、门禁命令、小程序约定）  
- `package.json`（`verify:gates`、`smoke:miniprogram`、`verify:ready`）  
- `scripts/verify-go-live-gates.mjs`（四门：test + critical + e2e + smoke）  
- `.github/workflows/ci.yml`  
- `miniprogram/pages/inbox/inbox.js`（已联调 `GET /v1/me/notifications`）  
- 延续文档：`全项目自动评审报告-2026-03-26.md`、`工程质量优化五项-工程评审-2026-03-27.md`、`文档漂移与上线治理-工程评审-2026-03-27.md`

### UI 范围判定

- **有 UI 范围**（小程序多页、Tab、分包、错误态）→ Design 维度需评审。

### 与 2026-03-26 报告的差异（事实更新）

- **Vitest：** 9 个文件、**38** 条用例通过（非 26）；含 `notifications-list`、`notifications-event-write`、`p1-listing-profile-filters` 等。  
- **消息：** 后端 `GET /v1/me/notifications` + 小程序 inbox 已走 **独立通知源**（非仅订单列表映射）。  
- **门禁脚本：** `verify:gates` 已包含 **小程序 smoke**。

---

## Phase 1 — CEO 评审（策略 / 范围）

### 前提（Premises — 若与业务不符请改本文件）

1. Cosii 当前阶段目标为 **可演示的交易/约妆/内容/争议主链 + 小程序可联调**，而非完整 C 端产品闭环。  
2. 生产环境 **不能接受** 长期依赖 `X-User-Id` 作为唯一身份源。  
3. **文档与代码** 允许短期漂移，但 **接口真源以 `src/**/routes.ts` + 测试为准**。

### 前提挑战（0A）

| 前提 | 评估 |
|------|------|
| 「门禁绿 = 可上线」 | **不成立** — 门禁未覆盖微信真机、支付真实回调、生产配置；仅证明 **后端逻辑与主链回归**。 |
| 「先堆功能再安全」 | **高风险** — `X-User-Id` 与 mock 开关在小程序侧一旦误入生产，后果大于多数功能缺口。 |

### 已有能力映射（0B / What already exists）

- 统一订单列表/摘要、交易详情、在售列表与上架、通知读写、内容+同城核销等：**后端 + 部分小程序** 已接线。  
- `verify:gates`、`verify:ready`、`docs/go-live-checklist.md`：**运维与自检入口已有。**

### 理想态差距（0C — 12 个月简图）

```text
CURRENT ──► 本仓库现状：主链自动化绿 + 小程序主干可演示
                │
                ▼
THIS PLAN ──► 下一里程碑：微信会话、生产密钥、CI=门禁对齐、契约测试/护栏（见工程质量五项）
                │
                ▼
12-MONTH ──► 规模化：真告警、SLO、多端一致、合规审计留痕
```

### 范围决策（自动采纳：完整优先 + 显式）

- **不缩减** 已记录的 P0（认证、生产治理）。  
- **工程质量五项**（parse 单测、mock 护栏、文档路由检查、trace runbook、错误模板）**不并入本报告实施**，见专项评审文档。

### CEO 双声道

- **Codex / 子代理：** 未执行 → 本节的「外部质疑」由主审根据常见盲区自行加压（见下表）。

### CEO 共识表（合成）

| 维度 | 主审结论 |
|------|----------|
| 前提是否成立？ | 开发阶段成立；上线前须替换身份模型 |
| 是否在做「对的问题」？ | 是 — 先固化主链与门禁，再换生产边界 |
| 范围是否校准？ | 文档已多轮回填；**CI 滞后于本地门禁** 需补一刀 |
| 竞品/时间风险？ | 低 — 垂直场景；风险在 **合规与支付** |

### Phase 1 — NOT in scope

- 大改版设计系统、重写后端框架。  
- 本文件不写具体微信开放平台配置步骤（以 checklist / runbook 为准）。

---

## Phase 2 — Design 评审（条件执行）

### Step 0 — 范围与 DESIGN.md

- 真源：`DESIGN.md` + `docs/designs/miniprogram-ui-shell-spec.md`。  
- 实现侧已广泛采用 **loading / error / 重试** 模式；**未在本轮做像素级对照 DESIGN.md**（若需，应单独开视觉 QA）。

### 维度速评（1–7，主观分）

| 维度 | 分 | 说明 |
|------|-----|------|
| 信息层级 | 7/10 | Tab + 列表 + 详情路径清晰 |
| 缺失状态 | 7/10 | 错误条与 toast 并存，可再 DRY |
| 旅程 | 6/10 | 发布/选图等仍偏工程演示 |
| 具体 vs 泛化 | 6/10 | 规格文档较细，实现仍在追赶 |
| 可访问性 | 5/10 | 小程序场景下待专项 |
| 与设计系统一致 | 6/10 | 需定期对照 DESIGN.md |
| 模糊点 | 空态文案、长列表性能未写死 | |

### Design 双声道

- 未执行 → 上表为主审合成。

### Phase 2 — NOT in scope

- 不重画整套组件库；不在本文件定义新色板。

---

## Phase 3 — Eng 评审

### Step 0 — Scope Challenge

- **子问题 → 代码：** 各模块 `routes.ts` + `service.ts` 模式统一；`src/shared/api-envelope`、`errors` 集中。  
- **复杂度：** 全项目级评审 **>8 文件** — 本报告 **只出结论与优先级**，不绑单 PR。  
- **TODOS.md：** 若存在未关闭项，应与 `go-live-checklist` 交叉 — 未强制改文件。

### 架构简图（ASCII）

```text
           ┌─────────────┐
  wx.request┤ miniprogram │  X-User-Id (dev)
           │  utils/api  │
           └──────┬──────┘
                  │ HTTP /v1/*
                  ▼
           ┌─────────────┐
           │   Fastify   │
           │  app.ts     │
           └──────┬──────┘
      ┌────────────┼────────────┐
      ▼            ▼            ▼
  trade/      booking/     me/ payment/ …
      │            │            │
      └────────────┴────────────┘
                  │
                  ▼
            node:sqlite (data/app.db)
```

- **单点：** SQLite 文件库 — 适合当前阶段；扩展时迁移策略需另文。  
- **安全：** 生产必须收口 dev header；`LOCAL_CODE_PEPPER` 等同密钥类 env。

### 代码质量要点

- **DRY：** 小程序多页 `cos-page-state` 重复 — 与《工程质量五项》条 2 一致。  
- **显式优于聪明：** 路由常量 `config/routes.js` 已体现，建议保持。

### 测试评审（Section 3 — 覆盖图摘要）

**框架：** Vitest + Playwright（见 `CLAUDE.md`）。

```text
CODE PATH COVERAGE（摘要）
===========================
[+] 后端主模块（trade/booking/me/payment/…）
    ├── 契约与回归测试        [★★★] 多文件覆盖
    ├── CRITICAL T11–T17      [★★★] critical-matrix
    └── 通知读写              [★★★] notifications-*.test.ts

[+] E2E 四条主链              [★★★] 4/4 passed

[+] 小程序                    [★★ ] smoke 静态/串行检查
    ├── parse 纯函数单测      [GAP] 见工程质量五项
    └── 真机自动化            [GAP] P2

USER FLOW COVERAGE
===========================
[+] 后端主链（经 E2E）        [★★★]
[+] 小程序关键路径            [★★ ] 以人工 + smoke 为主

─────────────────────────────────
COVERAGE 结论：后端与 E2E 强；小程序深测与契约单测偏弱。
─────────────────────────────────
```

### 测试计划产物（artifact）

- 本仓库策略：全项目评审的 **手工 QA 清单** 仍建议沿用 `docs/designs/eng-review-test-plan-miniprogram.md`（若存在）及 `go-live-checklist`。  
- gstack 路径 `~/.gstack/projects/cosii/*-test-plan-*.md` **未由本会话写入**（非阻塞）。

### 性能

- 当前无 N+1 类明显告警；SQLite 单文件在集成测试下足够。  
- 列表分页已见于 me/unified-orders；大表索引与迁移属后续。

### Eng 双声道

- 未执行。

### Eng 共识表（合成）

| 维度 | 主审结论 |
|------|----------|
| 架构 | 清晰，模块边界合理 |
| 测试 | 后端+E2E 强；小程序与 CI 对齐弱 |
| 性能 | 非瓶颈 |
| 安全 | **dev 认证与 mock** 为最大暴露面 |
| 错误路径 | envelope 一致性好 |
| 发布风险 | ~~CI 未跑 verify:gates~~ → **已与本地门禁对齐** |

### Phase 3 — Failure Modes（节选）

| 场景 | 测试 | 处理 | 用户可见 |
|------|------|------|----------|
| 生产仍信任 `X-User-Id` | 部分 E2E 用 dev header | 需产品化会话 | 账号串号 / 严重安全 |
| `useMockOrders` 误开 | 无硬闸脚本 | 待加护栏 | 假数据上生产 |
| CI 仅 npm test + e2e | CI 绿 | 与本地四门不一致 | 合并了「未跑 critical/smoke」的提交 |

**Critical gap（仍有效）：** 生产身份模型未切换前，**任何「门禁全绿」都不等价于生产安全**。

### Phase 3 — NOT in scope

- 不改具体业务规则数字（佣金、SLA）除非 checklist 要求。

### Phase 3 — What already exists（工程）

- `npm run verify:gates`、`verify:ready`、`smoke:miniprogram`。  
- `GET /v1/ready`、`STRUCTURED_ACCESS_LOG`、信封 `trace_id`。

---

## 跨阶段主题（Cross-Phase Themes）

1. **「本地比 CI 更严」** — 曾指向 P0-4；**已闭合**（CI 现跑 `verify:gates`）。后续若改门禁脚本，须同步改 `ci.yml`。  
2. **「信任边界」** — CEO（产品）+ Eng（安全）+ Design（若误用 dev 账号展示）同源。

---

## 决策审计轨迹（Autonomous Decision Log）

| # | Phase | 决策 | 原则 | 理由 |
|---|-------|------|------|------|
| 1 | CEO | 不缩减 P0 治理项 | P1 完整 | 上线阻塞真实存在 |
| 2 | CEO | 工程质量五项独立成文、不混写 | P3 务实 | 已有专项评审 md |
| 3 | Design | 不做像素级审计 | P5 显式 | 缺自动化设计 QA 工具链 |
| 4 | Eng | 优先建议对齐 CI 与 verify:gates | P2 煮沸湖面内 | 改动小、收益高 |
| 5 | Eng | 小程序 parse 单测跟进专项 | P1 完整 | 与 03-27 工程质量评审一致 |

---

## 与 GitHub CI 的差异（历史记录 → 已修复）

**曾发现（评审原文）：** `ci.yml` 仅 `npm test` + `test:e2e`，相对本地缺少独立 `test:critical` 步骤语义与 `smoke:miniprogram`。

**现状（2026-03-27 起）：** `npm ci` → `build` → `npx playwright install chromium --with-deps` → **`npm run verify:gates`**，与本地四门门禁一致。

**维护约定：** 新增/删减门禁时同时改 `scripts/verify-go-live-gates.mjs` 与 `.github/workflows/ci.yml`，并在 `CLAUDE.md`「测试与上线门禁」中写清。

---

## 优先级汇总（执行向 · 速览）

**详细任务表、验收标准与推荐顺序见上文「## 需要做什么（本评审 → 行动清单）」。**

- **P0：** 微信会话与生产鉴权；go-live 清单（密钥/支付/告警/日志）；~~CI ≈ verify:gates~~ **已完成**。  
- **P1：** mock 护栏、parse 单测、占位能力产品化、错误态 DRY、文档 vs routes 检查。  
- **P2：** 小程序自动化、trace_id runbook、设计抽检、DB 规模化策略。

---

## 附：本次评审证据

```text
$ git rev-parse --short HEAD
8cf2139

$ npm run verify:gates
✓ npm test — 9 files, 38 tests passed
✓ npm run test:critical — 7 tests passed
✓ npm run test:e2e — 4 passed
✓ npm run smoke:miniprogram — ok
```

---

## GSTACK REVIEW REPORT（合成）

| Review | Trigger | Why | Runs | Status | Findings |
|--------|---------|-----|------|--------|----------|
| CEO Review | /autoplan Phase 1 | 范围与风险 | 1 | issues_open | 生产边界未闭环 |
| Design Review | /autoplan Phase 2 | 小程序 UI | 1 | clean-ish | 无阻塞级视觉项 |
| Eng Review | /autoplan Phase 3 | 架构与测试 | 1 | issues_open | CI 已对齐；其余（parse 单测等）仍 open |
| Codex / 双声道 | — | 独立质疑 | 0 | — | 本报告未执行 |

**VERDICT：** **规划评审已落地为本文档**；**P0-4（CI 对齐）已在仓库落实**。后续优先 **P0-1～P0-3**（会话、清单、告警日志）。若需 gstack 仪表盘写入 `gstack-review-log`，请在本地执行官方 `/autoplan` 或手工跑 `gstack-review-log`。

---

## 相关文档

- [全项目自动评审报告-2026-03-26.md](./全项目自动评审报告-2026-03-26.md)  
- [工程质量优化五项-工程评审-2026-03-27.md](./工程质量优化五项-工程评审-2026-03-27.md)  
- [文档漂移与上线治理-工程评审-2026-03-27.md](./文档漂移与上线治理-工程评审-2026-03-27.md)
