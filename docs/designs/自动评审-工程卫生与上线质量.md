# 自动评审：工程卫生与上线质量（/autoplan）

评审日期：2026-03-26  
评审范围：仅针对本次指定内容（工程卫生、测试门禁、ready 探针、Go-Live 清单关键项）

---

## 1. 评审输入（用户指定）

1. 先做工程卫生  
   - 清理不该进仓库的产物（`dist/`、`node_modules/`、`miniprogram/node_modules/` 等）  
   - 补齐 `.gitignore`
2. 锁定后端可上线质量  
   - `npm test`  
   - `npm run test:critical`  
   - `npm run test:e2e`  
   - `GET /v1/ready`
3. 按 Go-Live 清单补缺  
   - 对照 `docs/go-live-checklist.md`  
   - 重点看：错误码映射、幂等、日志、trace 可追踪

---

## 2. 工程卫生评审结果

### 2.1 `.gitignore` 补齐（已完成）

本次新增忽略项：

- `playwright-report/`
- `miniprogram/node_modules/`
- `miniprogram/dist/`
- `miniprogram/project.private.config.json`
- `data/*.db*`
- `*.log`

保留原有：

- `node_modules/`
- `dist/`
- `test-results/`
- `.cursor/`
- `.gstack/`
- `project.private.config.json`

### 2.2 仓库噪音检查

- 当前 `git status --short` 仅剩：
  - `.gitignore`（已修改）
  - `docs/designs/自动评审三步结果-暂缓UI.md`（文档新增）
- `data/app.db`、`data/app.db-wal`、`data/app.db-shm` 已被忽略规则覆盖，不再污染评审。

结论：**工程卫生可用，噪音显著下降。**

---

## 3. 后端可上线质量评审结果（实跑）

### 3.1 全量测试

- 命令：`npm test`
- 结果：**通过**
- 摘要：4 个测试文件，23 个测试，全绿

### 3.2 关键矩阵

- 命令：`npm run test:critical`
- 结果：**通过**
- 摘要：`tests/critical-matrix.t11-t17.test.ts` 7 个测试全绿

### 3.3 E2E 四条主链

- 命令：`npm run test:e2e`
- 结果：**通过**
- 摘要：4 条链路全绿
  - 交易链
  - 约妆定金链
  - 争议审计链
  - 内容转化链

### 3.4 Ready / Health 探针

- `GET http://127.0.0.1:3000/v1/ready`：`200`，`data.ready=true`
- `GET http://127.0.0.1:3000/health`：`200`，`data.ok=true`
- 响应均包含 `trace_id`

结论：**当前环境下，后端基础可用性与主链路自动化门禁已满足。**

---

## 4. Go-Live 清单对照（本轮可确认项）

对照文档：`docs/go-live-checklist.md`

### 4.1 已验证（本轮可打勾）

- [x] `npm test` 绿  
- [x] `npm run test:critical` 绿  
- [x] `npm run test:e2e` 绿  
- [x] `GET /v1/ready` 可用（200，ready=true）  
- [x] `trace_id` 可追踪（ready/health 响应已见）

### 4.2 部分证据已具备，建议下一步补证

- [~] 回调幂等与乱序策略：critical 矩阵通过，可作为代码层证据；建议补充“发布环境演练记录”。
- [~] 错误码映射：已有测试与文档约束；建议补“人工抽样接口响应清单（含 `code/user_title/primary_action`）”。

### 4.3 本轮未验证（需发布前完成）

- [ ] 生产密钥、支付证书、回调 URL 与域名一致性
- [ ] 结构化访问日志 `STRUCTURED_ACCESS_LOG=1` 在生产开启并留样
- [ ] 告警路由（对账、结算死信、webhook 连续失败）
- [ ] 回滚演练与步骤文档
- [ ] 发布后抽样交易链/约妆链人工验证记录

---

## 5. 重点问题结论（按你要求）

### 错误码映射

- 结论：**中高信心通过**（测试与文档有约束）
- 缺口：发布前应补接口级抽样证据（不是只看自动测）

### 幂等

- 结论：**通过**（critical + e2e 已覆盖关键路径）
- 缺口：建议增加一次真实部署环境回放/重放演练记录

### 日志

- 结论：**待补证**
- 原因：本轮未在生产配置下验证结构化日志开启与字段完整性

### trace 可追踪

- 结论：**通过（本地环境）**
- 证据：`/v1/ready` 与 `/health` 响应均返回 `trace_id`

---

## 6. 评审结论与下一步

评审状态：**DONE_WITH_CONCERNS**

- 已完成：工程卫生、三类测试门禁、ready 探针验证
- 仍需收口：生产侧日志/告警/密钥/回滚证据链

建议按优先级执行：

1. 先补 `go-live-checklist` 未验证项的“可审计证据”（截图/日志/演练记录）
2. 产出一份“发布前 30 分钟检查单”（可直接操作）
3. 再决定是否恢复 UI 线（避免并行分散质量注意力）

---

## 7. 落实记录（工程实现）

以下已写入仓库，便于复跑与发布收口：

| 项 | 路径 / 命令 |
|----|-------------|
| 一键三门禁 | `npm run verify:gates` → `scripts/verify-go-live-gates.mjs` |
| 探活脚本 | `npm run verify:ready` → `scripts/check-ready.mjs`（`API_BASE` 可选） |
| 发布前 30 分钟 | `docs/发布前30分钟检查单.md` |
| 错误体抽样对照 | `docs/designs/错误响应抽样清单.md` |
| 回滚操作细则 | `docs/runbooks/发布回滚步骤.md` |
| 清单勾选更新 | `docs/go-live-checklist.md`（自动化门禁、探活、trace、幂等代码验证等已勾选并附引用） |

