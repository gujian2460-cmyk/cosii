# 《文档漂移与上线治理 — 工程评审》

- **评审类型：** `/plan-eng-review` 风格（范围 / 架构与运维 / 质量 / 测试与证据 / 失败模式）
- **输入范围：** `docs/designs/全项目自动评审报告-2026-03-26.md` **第二节 B（文档漂移）** 与 **第二节 C（上线治理未闭环）**（原稿约 61–71 行）
- **评审日期：** 2026-03-27
- **代码对照基线：** 仓库当前实现（`GET /v1/me/unified-orders`、`subPackages`、`GET /v1/me/notifications`、`user_notifications`、`verify:gates` 含 `smoke:miniprogram` 等）

---

## Step 0 — Scope Challenge

### 0.1 子问题拆解

| 子问题 | 是否已有代码/文档承接 | 本次评审产出 |
|--------|------------------------|--------------|
| B-1 `autoplan-five-tabs` §14 仍写「订单列表 API 关键缺口」 | **否** — 与现状不符；`me` 模块与小程序订单页已联调 | 列出**应改段落**与**新表述要点** |
| B-2 `miniprogram-ui-shell-spec` 仍写「分包未在 app.json 声明、计划延后」 | **否** — `miniprogram/app.json` 已含 `packageTrade` / `packageBooking` / `packageContent` | 列出**应改表格行**与**Decisions Log 建议** |
| C 上线治理三项 | **部分** — `docs/go-live-checklist.md` 已结构化勾选；多项仍为 `[ ]` | **映射到 checklist 条目** + **执行顺序** + **证据要求**；**不写业务代码** |

### 0.2 最小变更集

- **文档：** 2 个设计文件内**局部修订**（非重写全文）；建议在修订处加 **「2026-03-27 对齐代码」** 脚注或 Decisions Log 一行，避免读者误以为整篇已全盘更新。
- **治理：** 以 `go-live-checklist.md` 为**唯一勾选真源**；全项目自动评审报告 C 节可改为「见 checklist §X」减少双处维护。

### 0.3 复杂度

- 文档修订：**&lt;8 段**即可消除报告 B 所指矛盾；属**可煮沸湖面**。
- 上线治理 C：**运维/流程工作**，与功能开发正交；不宜与「改 autoplan 一句话」混在同一 PR 认知里。

**MODE：** `FULL_REVIEW`（文档与治理都谈透；**不**在本文件中实现微信登录或告警系统）。

---

## 1. Architecture Review（信息与运维架构）

### 1.1 文档真源关系（现状问题）

```text
DESIGN.md ─────────────► 视觉/token 真源
        │
        ├── miniprogram-ui-shell-spec.md ──► 壳、请求、Tab 语义
        ├── autoplan-five-tabs-pages-layout.md ──► 页面布局与 eng 执行清单（含 §14）
        └── CLAUDE.md ──► 运行时与 API 清单（已多次更新）

漂移风险：autoplan §14 写成「评审落盘」后若**不随代码迭代打补丁**，读者会以为「订单列表仍无 API」。
```

**建议（显式优于聪明）：**

1. 在 `autoplan-five-tabs-pages-layout.md` **§14 文首**增加 **Status 横幅**：「§14.6 及 Completion 表中 Critical gap 描述截至 2026-03-27 前有效；列表/通知/分包已与代码对齐，以下小节保留历史分阶段建议，以 **CLAUDE.md + 仓库路由** 为准。」  
   - **备选（更干）：** 直接改写 §14.1 表格行、§14.6、§14.13 的 Critical gap 行（见下文 §8）。

2. `miniprogram-ui-shell-spec.md` **§13.3 表格第 3 行**改为 **「已落地」**，并指向 `app.json` 的 `subPackages` 与 `config/routes.js`。

### 1.2 上线治理架构

```text
配置密钥 ──► 运行环境（Fly/自建等）
数据迁移 ──► schema.sql / 备份
可观测性 ──► STRUCTURED_ACCESS_LOG + 平台留样 + 告警路由
身份边界 ──► 生产禁用纯 X-User-Id（与小程序 session/微信并行）
```

**单点失败：** 若仅改文档勾选而**未**做密钥与身份切换，生产仍为**高误配置面**。

---

## 2. Code Quality Review（文档与清单质量）

### 2.1 具体问题定位

**B-1 — `autoplan-five-tabs-pages-layout.md`**

| 位置 | 过时内容 | 建议新语义（摘要） |
|------|----------|---------------------|
| §12 | 「后续按 §5 订单列表…补齐」 | 订单列表、筛选、详情深链、消息通知 API **已存在**；余下为体验打磨（空态 copy、图片上传等） |
| §14.1 表格 | 「没有统一订单列表 HTTP 接口」 | **已有** `GET /v1/me/unified-orders`（`order_type` / `status` / cursor） |
| §14.6 标题与正文 | 「关键缺口…无列表」 | 改为 **「已实现」** 或移入 **「历史：曾阻塞 §5」** 附录 |
| §14.7 USER FLOW | 订单列表→详情 `[GAP]` | 标注 **API + 分包已具备**；E2E 仍以 Playwright API 为主，小程序运行时自动化另册 |
| §14.9 阶段 B | 「后端 GET 列表」为待做 | 标为 **完成** 或合并到后续「增强项」 |
| §14.13 | `Critical gap | 订单列表无后端接口` | **删除或改为 resolved**，避免与自动评审报告冲突 |
| §14.14 TODOS | P1 列表 API | **改为 done** 或指向「分页/筛选增强」 |

**B-2 — `miniprogram-ui-shell-spec.md`**

| 位置 | 过时内容 | 建议 |
|------|----------|------|
| §5.2 目录树 | `packages/` 命名与仓库 `packageTrade` 等不一致 | 加脚注：**实际 root 名以 `app.json` 为准** |
| §13.3 行 3 | 「分包未声明、计划延后」 | 改为 **已声明 subPackages**；MVP 仅主包 Tab 的表述改为 **「首版可仅主包」已演进为「主包+三分包」** |

### 2.2 DRY

- **全项目自动评审报告** B 节在修订上述两文后，可压缩为：「详见各设计文档 2026-03-27 修订说明」，减少三份文档重复叙述同一缺口。

---

## 3. Test Review（证据与门禁）

### 3.1 与 B/C 相关的「测试」= **可验证证据**

| 主题 | 已有自动化 | 建议证据（上线前） |
|------|------------|---------------------|
| 订单列表 API | `tests/me-unified-orders.test.ts`、`p1-listing-profile-filters.test.ts` | CI 链接或发布说明中引用 `verify:gates` |
| 文档与代码一致 | **无** | **人工 diff 或 PR 描述**中列出「已改 autoplan §14 / ui-shell §13.3」 |
| C-1 密钥 | 无自动化 | 配置截图/导出**脱敏**存 wiki；**勿**提交仓库 |
| C-2 日志与告警 | 代码具备 `STRUCTURED_ACCESS_LOG` | 平台侧截图/告警规则 ID 登记到 runbook |
| C-3 身份 | 开发期 `session` + `X-User-Id` | 生产环境变量与网关层**剥离** dev header 的说明 |

### 3.2 覆盖示意（仅文档/治理）

```
[★★★] go-live-checklist 条目与代码能力对齐 — 已较好
[★★ ] 设计文档与 CLAUDE.md / 路由 同步 — GAP（本次评审要求修补）
[★   ] 生产告警与日志留样 — 纯运维 GAP
```

---

## 4. Performance Review

- 文档修订：无性能影响。
- 治理项 C-2：结构化日志若全量打满，注意**采样或按路径过滤**，避免热路径磁盘爆量（属运维策略，非本仓库必选代码变更）。

---

## 5. Failure Modes

| 失败模式 | 测试/证据 | 处理 | 用户/团队可见 |
|----------|-----------|------|----------------|
| 设计文档仍写「无列表 API」 | 无自动检测 | 新同学按文档排错优先级 | **静默错误（高）** — 浪费迭代 |
| checklist 未勾选但强行上线 | 发布评审漏看 | 发布窗口短清单 + 负责人签字 | **事故级（高）** |
| 仅关 C-3 未关 C-1 | 渗透/误操作 | 密钥与身份**并列** P0 | 资金与数据风险（高） |

**Critical gap（延续原报告）：** C-3「禁用纯 `X-User-Id`」未完成前，**任何「已登录」小程序体验在生产上均为假闭环** — 与 B 漂移无关但必须与 go-live **同一优先级队列**。

---

## 6. NOT in scope（本评审文件不包）

- 实现微信 `code2session`、服务端 session 表或 JWT。
- 配置具体云厂商告警规则与值班表正文（仅指出应对齐 checklist）。
- 重写 `autoplan` 全文或合并多份设计为单文件。

---

## 7. What already exists（可复用）

- `GET /v1/me/unified-orders`、`GET /v1/me/unified-orders/:id`、`GET /v1/me/profile`、`GET /v1/me/notifications`
- `miniprogram/app.json` 的 `subPackages` 与 `config/routes.js`
- `docs/go-live-checklist.md`（含 `[x]` / `[ ]` 与 runbook 引用）
- `npm run verify:gates`（含 `smoke:miniprogram`）

---

## 8. 建议修订清单（供直接开 PR 勾选）

- [x] `autoplan-five-tabs-pages-layout.md`：§12、§14 文首 Status、§14.1、§14.6–§14.7、§14.9、§14.13–§14.14 已与代码对齐（2026-03-27）。
- [x] `miniprogram-ui-shell-spec.md`：§5.2 与仓库目录/`app.json` 对齐说明；§13.3 第 3 行已标为已落地（2026-03-27）。
- [x] `全项目自动评审报告-2026-03-26.md`：B 节已改为「已回填」并链回本文；P0「文档对齐」已划掉。
- [x] `go-live-checklist.md`：文首已注明为自动评审 C 节**唯一详细真源**。

---

## 9. Completion Summary

- **Step 0：** 范围接受 — 文档局部修订 + 治理映射 checklist，不混入功能开发。
- **Architecture：** 2 条（真源分层、治理流水线）
- **Code quality（文档）：** 2 份文件、多段落过时；已表格化定位
- **Test / 证据：** 自动化覆盖 API；文档一致性与运维证据仍靠流程
- **Performance：** 1 条运维提示
- **NOT in scope / What exists：** 已写
- **Failure modes：** 1 条 critical（生产身份）
- **Outside voice：** skipped（单模型评审落盘）
- **总评：** **DONE_WITH_CONCERNS** — B 为**低成本高收益**修订；C 为**运维 P0**，与文档修正独立并行即可。

---

## 10. 与 `go-live-checklist.md` 的映射（C 节）

| 原报告 C 条 | Checklist 锚点 |
|-------------|----------------|
| 生产密钥与支付配置 | 「配置与密钥」整节 |
| 日志留样与告警 | 「可观测性与告警」中 `[ ]` 子项 |
| 禁用纯 `X-User-Id` | 「配置与密钥」第 4 条 + 小程序 `api.js`/微信方案 |

**建议执行顺序：** 密钥与 DB 路径（C-1）→ schema 应用（数据节）→ 身份边界（C-3）→ 日志留样与告警（C-2），与「回滚」「发布后验证」串联阅读 `docs/runbooks/发布回滚步骤.md` 与 `docs/发布前30分钟检查单.md`。
