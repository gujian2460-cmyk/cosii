# 自动评审三步结果（暂缓 UI）

评审时间：2026-03-26  
评审方式：`/autoplan` 三步框架（CEO / Design / Eng）  
当前策略：**先不做 UI，实现顺序切换为后端稳定性与上线门禁优先**

---

## 0. 评审输入

- `DESIGN.md`
- `docs/designs/miniprogram-ui-shell-spec.md`
- `docs/designs/ui-outsourcing-brief.md`
- `docs/designs/eng-review-test-plan.md`
- `TODOS.md`

---

## 1) CEO 评审（范围与业务优先级）

### 结论

- **OK：** UI 外包方案文档完整，范围、里程碑、验收标准可执行。
- **WARNING：** 当前阶段若继续推进 UI，会与“先验证交易/履约稳定性”的业务优先级冲突。
- **决策：** 本轮执行 **HOLD SCOPE + 暂缓 UI 实施**，保留 UI 文档作为后续启用资产。

### 核心判断

1. 当前价值最大化路径不是“更快出页面”，而是“先把交易/支付/履约链路打稳”。  
2. 若 UI 先行，风险是形成“视觉完成感”，但关键后端质量门禁未完成。  
3. 现有文档资产足够，暂缓不会导致设计知识丢失。

### CEO 决策

- 采纳：将 UI 作为 **已准备好但未启用** 的工作流。
- 暂缓：小程序视觉落地、外包执行、UI 走查会议。
- 立即执行：测试门禁、上线清单、工程卫生。

---

## 2) Design 评审（设计完整性与可落地性）

### 结论

- **OK：** 设计真源一致（`DESIGN.md` + UI shell spec），反 AI-slope 约束明确。
- **OK：** 外包委托文档具备交付闭环（交付物、里程碑、验收 checklist、风险规避）。
- **WARNING：** 文档中仍包含少量“TS 落地假设”，而当前小程序工程已切换 `.js` 运行时，后续恢复 UI 时需一次对齐检查。

### Design 风险（已记录）

1. 若恢复 UI 开发时不做规范同步，可能出现文档与实现偏差。  
2. 若先做视觉再补状态页，支付/争议/核销异常路径容易被弱化。  

### Design 决策

- 保持设计资产冻结，不继续扩写视觉细节。  
- 恢复 UI 前先做一次“规范与运行时一致性检查”（文档 -> 工程）。

---

## 3) Eng 评审（工程风险、测试与上线准备）

### 结论

- **CRITICAL GAP：** 若现在不优先跑通并固定测试门禁，后续 UI 恢复会放大联调风险。  
- **OK：** `eng-review-test-plan.md` 已给出主链和边界条件，适合直接作为后端验证清单。  
- **OK：** 当前工作区状态可用于“先做非 UI 冲刺”（无明显未提交冲突）。  

### Eng 最小可执行路径（非 UI）

1. 工程卫生：确认忽略规则和产物管理（`node_modules/`、`dist/`、测试产物）。  
2. 质量门禁：依次跑
   - `npm test`
   - `npm run test:critical`
   - `npm run test:e2e`
3. 上线门禁：对照 `docs/go-live-checklist.md` 完成逐项勾选。  
4. 运行态检查：`/v1/ready`、结构化访问日志、错误码映射抽查。  

### Failure Modes（当前最应防）

| 风险 | 影响 | 当前动作 |
|---|---|---|
| 支付回调重复/乱序处理不稳 | 订单状态错乱、资金风险 | 优先跑 critical + e2e 链路 |
| 档期并发占位冲突 | 双约、履约纠纷 | 优先验证 booking 并发用例 |
| 争议/结算异常路径不可见 | 客诉升级、定位困难 | 验证错误码映射与 trace 展示策略 |
| UI 先行导致后端门禁滞后 | 集成延期、返工增加 | 明确暂缓 UI，先完成后端门禁 |

---

## 综合判定

**状态：DONE_WITH_CONCERNS**

- 三步评审完成，结论一致：**当前阶段应先做非 UI 冲刺**。  
- UI 方案不是废弃，而是转为“冻结待启用资产”。  
- 触发恢复 UI 的前置条件：后端测试门禁全绿 + go-live 清单达标。

---

## 下一步（按优先级）

### P0（今天）

1. 跑完三套测试门禁并记录结果。  
2. 对照 `go-live-checklist.md` 打勾并标注阻塞项。  

### P1（本周）

1. 补齐阻塞项（错误映射、日志、幂等、ready 探针）。  
2. 更新 `TODOS.md`：将 UI 标记为“待门禁通过后恢复”。  

### P2（恢复 UI 前）

1. 做一次“UI 文档与当前工程（`.js` 运行时）一致性检查”。  
2. 再启动外包执行与设计走查会。  

---

## 附：本次不做的事（NOT in scope）

- 不新增 UI 页面与视觉稿实现。  
- 不启动 UI 外包执行节奏（M1~M4 暂缓）。  
- 不扩写新的视觉规范章节。  

