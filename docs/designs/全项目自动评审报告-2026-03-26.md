# 《Cosii 全项目自动评审报告（中文）》

- **评审时间：** 2026-03-26
- **评审范围：** 后端 `src/`、测试 `tests/ + e2e/`、小程序 `miniprogram/`、上线文档 `docs/`
- **评审方式：** 代码与文档静态审阅 + 门禁实跑（`npm run verify:gates`）
- **当前基线：** `HEAD=8cf2139`

---

## 一、已经完成（可验收）

### 1) 三道发布门禁通过

- [x] `npm test` 通过（26 tests）
- [x] `npm run test:critical` 通过（T11–T17）
- [x] `npm run test:e2e` 通过（4 条主链）
- [x] `npm run verify:gates` 一键门禁通过

### 2) 后端能力补齐（相对五 Tab 方案）

- [x] 新增小程序订单列表接口：`GET /v1/me/unified-orders?limit=&cursor=`
- [x] 新增小程序订单摘要接口：`GET /v1/me/unified-orders/:unifiedOrderId`
- [x] 新增闲置订单详情接口：`GET /v1/trade/orders/:tradeOrderId`
- [x] 接口鉴权与 envelope 语义保持一致（`X-User-Id` + `ok/fail`）
- [x] 新增回归测试：`tests/me-unified-orders.test.ts`

### 3) 小程序主干落地

- [x] 五 Tab 主包结构保留，顺序与设计决策一致
- [x] 新增分包声明（trade/booking/content）并落地目录骨架
- [x] `config/routes.js` 路径常量化（避免字符串散落）
- [x] `utils/errors.js` 增加页面错误映射
- [x] `utils/api.js` 补齐「2xx 但非法 envelope」兜底
- [x] 首页、订单、消息实现 loading/error/正文分层
- [x] 订单列表可拉取后端数据并分页，行点击进入详情页
- [x] 消息页从“跳订单 Tab”升级为“优先深链订单详情”

### 4) 文档与运行手册已有基础

- [x] `docs/go-live-checklist.md`、`docs/发布前30分钟检查单.md`、`docs/runbooks/发布回滚步骤.md` 已存在
- [x] 小程序实现规格与测试计划文档已存在（`docs/designs/*`）

---

## 二、未完成（当前不是“全量完成态”）

### A. 小程序仍有占位逻辑（功能未闭环）(done)

1. **发布链路（2026-03-27 起部分闭环）**
   - `pages/publish` → 分包 `packageTrade/pages/publish-item` 表单 + `POST /v1/trade/items`；`packageContent/pages/entry` 可 `POST /v1/posts`（占位图 URL）。真机图片上传与微信登录仍待产品化。
2. **分包 booking/content（2026-03-27 起最小可用）**
   - `packageBooking/pages/entry` 可填档期 ID + 金额并 `POST /v1/booking/orders`；`packageContent/pages/entry` 可发图文帖。档期列表 API、选图上传等仍可迭代。
3. **在售列表**
   - `packageTrade/pages/list` 已联调 `GET /v1/trade/items`（非占位页）。
4. **订单筛选（2026-03-27 起）**
   - `order_type` 与 `status` 均已传 `GET /v1/me/unified-orders`；状态为横向 chips（常用枚举）。
5. **“我的”页身份（2026-03-27 起开发期闭环）**
   - `utils/session.js` + 本地覆盖 user_id；`api.js` 统一注入 `X-User-Id`。生产仍须接微信 code2session，禁用纯 dev header。

### B. 文档状态与代码状态（2026-03-27 已回填）done

1. ~~`autoplan-five-tabs-pages-layout.md` §14 订单列表缺口~~ **已修订**：文首 Status、§12、§14.1 / §14.6–§14.7 / §14.9 / §14.13–§14.14 已与实现对齐；详见 [文档漂移与上线治理-工程评审-2026-03-27.md](./文档漂移与上线治理-工程评审-2026-03-27.md)。
2. ~~`miniprogram-ui-shell-spec.md` 分包延后~~ **已修订**：§5.2 目录与 `app.json` 命名对齐说明；§13.3 第 3 行标为已落地。

### C. 上线治理项未闭环（go-live checklist 仍有未勾选）

1. 生产密钥与支付配置核对未完成（`DB_PATH`/`LOCAL_CODE_PEPPER`/微信支付证书与回调）。
2. 生产日志留样、告警路由（结算死信、webhook 连续失败）未完成。
3. 生产环境“禁用纯 `X-User-Id` 信任边界”未完成（需微信会话接入）。

---

## 三、还需要做什么（按优先级）

### P0（上线前必须）

1. **完成认证边界切换**：从开发态 `X-User-Id` 过渡到真实微信会话。
2. **清理或收敛占位路径**：至少保证“发布商品/发布作品/在售列表”三条路径不出现“到处 toast 占位”。
3. ~~**文档对齐代码**~~：autoplan / ui-shell 已与实现对齐（2026-03-27）；新增文档仍须按 PR 同步 `CLAUDE.md`。
4. **补齐 go-live checklist 未勾选项**：密钥、告警、日志留样、回滚演练证据。

### P1（一周内建议完成）done

1. ~~订单筛选真实联调~~（已完成 `order_type` + `status`）。
2. ~~“我的”页开发期账号切换 + profile~~；生产环境微信会话与禁用纯 `X-User-Id` 仍属 P0。
3. ~~`packageTrade/list` 在售列表~~（已联调）；可继续做筛选增强与详情转化动线打磨。

### P2（优化性迭代）done

1. 消息来源从“订单列表映射”升级到独立通知数据源。
2. 加入小程序自动化（miniprogram-automator 或真机回归脚本）。

---

## 四、还可以优化什么（工程质量）done

1. **API 与客户端契约测试再加一层**
   - 为小程序请求层增加纯函数级解析测试（`parseEnvelope`）与契约快照。
2. **错误体验统一**
   - 除 toast 外，统一页面错误条样式与重试行为（当前已部分完成，可进一步复用组件/模板）。
3. **可观测性前移**
   - 将 `trace_id` 与关键业务事件（下单/支付/争议）建立一键串联查询说明文档。
4. **发布安全护栏**
   - 增加“生产构建禁用 `useMockOrders`”检查脚本，防止 mock 开关误上生产。
5. **文档一致性自动检查**
   - 在 CI 增加“接口清单文档与 routes 差异检查”脚本，降低“文档过时”风险。

---

## 五、总体结论

**结论：DONE_WITH_CONCERNS（已完成核心主干，但尚未达到“全量完成可直接上线”）**。

- 核心后端链路与自动化门禁质量是好的（全部通过）。
- 小程序已从“壳”走到“可联调主路径”，但仍有占位页面和未闭环交互。
- 当前最关键的上线阻塞不在“代码能不能跑”，而在**认证边界、占位收敛、生产运维治理与文档回填**。

---

## 六、附：本次评审证据

- `npm run verify:gates`：通过
- `tests/me-unified-orders.test.ts`：通过
- 新增/已用接口：
  - `/v1/me/unified-orders`
  - `/v1/me/unified-orders/:unifiedOrderId`
  - `/v1/trade/orders/:tradeOrderId`
- 小程序已落地：分包声明、路由常量、订单列表拉数、消息深链详情
