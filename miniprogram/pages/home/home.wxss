/* 首页：品牌 #D878A0 / 底 #F5F6F8 — 与 app.wxss 变量一致 */

.home-page {
  width: 100%;
  min-height: 100vh;
  background-color: var(--color-bg);
}

.home-header {
  position: sticky;
  top: 0;
  z-index: 200;
  background-color: var(--color-bg);
  box-sizing: border-box;
}

.home-header__status {
  width: 100%;
}

.home-header__row {
  display: flex;
  flex-direction: row;
  align-items: center;
  padding-left: 24rpx;
  gap: 16rpx;
  box-sizing: border-box;
}

.home-search-capsule {
  flex: 1;
  min-width: 0;
  height: 64rpx;
  padding: 0 28rpx;
  margin-right: 0;
  background-color: #eceef2;
  border-radius: var(--radius-pill);
  display: flex;
  align-items: center;
  box-sizing: border-box;
}

.home-recent-expo {
  flex-shrink: 0;
  padding: 0 20rpx;
  height: 56rpx;
  line-height: 56rpx;
  font-size: 24rpx;
  font-weight: 600;
  color: var(--color-primary-dark);
  background-color: #fff0f5;
  border-radius: var(--radius-pill);
  border: 1rpx solid rgba(216, 120, 160, 0.35);
}

.home-search-capsule__ph {
  font-size: 24rpx;
  color: var(--color-text-muted);
}

.home-scroll {
  width: 100%;
  box-sizing: border-box;
}

.home-banner-wrap {
  padding: 0 24rpx;
  margin-top: 8rpx;
}

.home-banner-swiper {
  height: 280rpx;
  border-radius: 16rpx;
  overflow: hidden;
}

.home-banner-slide {
  height: 100%;
  border-radius: 16rpx;
  padding: 32rpx 28rpx;
  box-sizing: border-box;
  display: flex;
  flex-direction: column;
  justify-content: flex-end;
}

.home-banner-slide__k {
  font-size: 24rpx;
  color: rgba(0, 0, 0, 0.55);
  margin-bottom: 8rpx;
}

.home-banner-slide__t {
  font-size: 32rpx;
  font-weight: 700;
  color: #111111;
  line-height: 1.3;
}

.home-dots {
  display: flex;
  flex-direction: row;
  justify-content: center;
  align-items: center;
  gap: 12rpx;
  margin-top: 16rpx;
}

.home-dots__item {
  width: 12rpx;
  height: 12rpx;
  border-radius: 50%;
  background-color: #d0d0d0;
}

.home-dots__item--on {
  width: 24rpx;
  border-radius: 6rpx;
  background-color: #d878a0;
}

.home-king-scroll {
  width: 100%;
  white-space: nowrap;
  margin-top: 8rpx;
}

.home-king {
  display: inline-flex;
  flex-direction: row;
  padding: 32rpx 24rpx 8rpx;
  gap: 28rpx;
  background-color: transparent;
}

.home-king--5 {
  min-width: 100%;
  box-sizing: border-box;
}

.home-king__item {
  flex-shrink: 0;
  width: 120rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
}

.home-king__circle {
  width: 80rpx;
  height: 80rpx;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 32rpx;
  font-weight: 700;
  color: #ffffff;
  margin-bottom: 12rpx;
}

.home-king__circle--1 {
  background: linear-gradient(145deg, #d878a0, #e8a0bc);
}

.home-king__circle--2 {
  background: linear-gradient(145deg, #c45d88, #d878a0);
}

.home-king__circle--3 {
  background: linear-gradient(145deg, #9b6b86, #d878a0);
}

.home-king__circle--4 {
  background: linear-gradient(145deg, #d878a0, #f0b8cc);
}

.home-king__circle--5 {
  background: linear-gradient(145deg, #8b5a7c, #d878a0);
}

.home-king__label {
  font-size: 22rpx;
  color: #111111;
  text-align: center;
  line-height: 1.25;
  margin-top: 4rpx;
}

.home-feed-head--tight {
  padding-top: 24rpx;
}

.home-char-scroll {
  width: 100%;
  white-space: nowrap;
  margin-bottom: 8rpx;
}

.home-char-inner {
  display: inline-flex;
  flex-direction: row;
  padding: 0 20rpx 16rpx;
  gap: 24rpx;
}

.home-char-chip {
  flex-shrink: 0;
  width: 132rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
}

.home-char-ava {
  width: 100rpx;
  height: 100rpx;
  border-radius: 50%;
  border: 3rpx solid #ffffff;
  box-shadow: 0 4rpx 12rpx rgba(216, 120, 160, 0.2);
}

.home-char-name {
  margin-top: 10rpx;
  font-size: 26rpx;
  font-weight: 600;
  color: #111111;
}

.home-char-work {
  margin-top: 4rpx;
  font-size: 22rpx;
  color: var(--color-text-muted);
  text-align: center;
  width: 100%;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.feat-card {
  border-radius: 16rpx;
  overflow: hidden;
  margin-bottom: 20rpx;
  box-shadow: 0 4rpx 20rpx rgba(0, 0, 0, 0.06);
}

.feat-card__media {
  position: relative;
  width: 100%;
  min-height: 280rpx;
  border-radius: 16rpx;
}

.feat-card__grad {
  position: absolute;
  left: 0;
  right: 0;
  bottom: 0;
  padding: 48rpx 16rpx 16rpx;
  background: linear-gradient(180deg, transparent 0%, rgba(0, 0, 0, 0.55) 100%);
  border-radius: 0 0 16rpx 16rpx;
  display: flex;
  flex-direction: column;
  gap: 6rpx;
}

.feat-card__char {
  font-size: 30rpx;
  font-weight: 800;
  color: #ffffff;
}

.feat-card__sub {
  font-size: 24rpx;
  color: rgba(255, 255, 255, 0.92);
}

.feat-card__row {
  display: flex;
  flex-direction: row;
  align-items: center;
  justify-content: space-between;
  margin-top: 8rpx;
}

.feat-card__auth {
  font-size: 22rpx;
  color: rgba(255, 255, 255, 0.88);
}

.feat-card__like {
  font-size: 22rpx;
  color: #ffffff;
  font-weight: 600;
}

.home-feed-head {
  padding: 32rpx 20rpx 16rpx;
  display: flex;
  flex-direction: row;
  align-items: baseline;
  gap: 16rpx;
}

.home-feed-head__title {
  font-size: 32rpx;
  font-weight: 700;
  color: #111111;
}

.home-feed-head__sub {
  font-size: 24rpx;
  color: var(--color-text-muted);
}

.home-masonry {
  display: flex;
  flex-direction: row;
  align-items: flex-start;
  padding: 0 20rpx 32rpx;
  gap: 20rpx;
}

.home-masonry__col {
  flex: 1;
  min-width: 0;
}

.feed-card {
  background-color: #ffffff;
  border-radius: 16rpx;
  overflow: hidden;
  margin-bottom: 20rpx;
  box-shadow: 0 4rpx 20rpx rgba(0, 0, 0, 0.04);
}

.feed-card__media {
  width: 100%;
  overflow: hidden;
}

.feed-card__img {
  width: 100%;
  display: block;
  height: 320rpx;
  border-top-left-radius: 16rpx;
  border-top-right-radius: 16rpx;
  border-bottom-left-radius: 0;
  border-bottom-right-radius: 0;
}

.feed-card__img--ph {
  height: 280rpx;
}

.feed-card__body {
  padding: 16rpx 16rpx 20rpx;
}

.feed-card__title {
  display: -webkit-box;
  -webkit-box-orient: vertical;
  -webkit-line-clamp: 2;
  overflow: hidden;
  font-size: 28rpx;
  font-weight: 600;
  color: #111111;
  line-height: 1.4;
}

.feed-conv {
  margin-top: 12rpx;
  padding: 10rpx 14rpx;
  border-radius: 8rpx;
  align-self: flex-start;
}

.feed-conv__text {
  font-size: 24rpx;
  font-weight: 500;
}

.feed-conv--product {
  background-color: #fff0f5;
}

.feed-conv--product .feed-conv__text {
  color: #b03060;
}

.feed-conv--booking {
  background-color: #f3e8ff;
}

.feed-conv--booking .feed-conv__text {
  color: #6b21a8;
}

.feed-card__foot {
  margin-top: 16rpx;
  display: flex;
  flex-direction: row;
  align-items: center;
}

.feed-card__ava {
  width: 32rpx;
  height: 32rpx;
  border-radius: 50%;
  overflow: hidden;
  flex-shrink: 0;
  display: flex;
  align-items: center;
  justify-content: center;
}

.feed-card__ava-img {
  width: 100%;
  height: 100%;
}

.feed-card__ava-t {
  font-size: 18rpx;
  color: #666666;
}

.feed-card__nick {
  margin-left: 8rpx;
  font-size: 24rpx;
  color: var(--color-text-muted);
  flex: 1;
  min-width: 0;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.feed-card__v {
  margin-left: 8rpx;
  flex-shrink: 0;
  width: 28rpx;
  height: 28rpx;
  border-radius: 50%;
  background: linear-gradient(145deg, #f59e0b, #fbbf24);
  color: #fff;
  font-size: 18rpx;
  font-weight: 800;
  display: flex;
  align-items: center;
  justify-content: center;
  line-height: 1;
}

.home-foot {
  padding: 24rpx 32rpx 48rpx;
  border-top: 1rpx solid var(--color-border);
}

.home-foot__t {
  font-size: 24rpx;
  color: var(--color-text-muted);
  display: block;
}

.home-foot__trace {
  font-size: 22rpx;
  color: #bbbbbb;
  margin-top: 8rpx;
  word-break: break-all;
}

.home-foot-spacer {
  height: calc(24rpx + env(safe-area-inset-bottom));
}
